# Load packaged station metadata

Internal helper used by download wrappers when `station_data` is not
supplied.

## Usage

``` r
.load_station_metadata()
```

## Value

Data frame of station metadata.
