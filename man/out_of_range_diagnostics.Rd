% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Range_Diagnostics.R
\name{out_of_range_diagnostics}
\alias{out_of_range_diagnostics}
\title{Diagnose Out-of-Range Values Using Baseline Variable Ranges}
\usage{
out_of_range_diagnostics(
  base_dir = getwd(),
  db_name = "climate_database.db",
  out_dir = file.path(base_dir, "drifloon_output", "diagnostics"),
  station_ids = NULL,
  province = NULL,
  years = NULL,
  variables = c("Temp_C", "Dew_Point_C", "Rel_Hum", "Wind_Dir_deg", "Wind_Spd_kmh",
    "Visibility_km", "Stn_Press_kPa", "Hmdx", "Wind_Chill"),
  canada_ranges_rds_path = file.path(base_dir, "data",
    "variable_ranges_Canada_1980-2020.rds"),
  province_ranges_rds_path = file.path(base_dir, "data",
    "variable_ranges_province_1980-2020.rds"),
  include_station_breakdown = FALSE,
  include_outlier_rows = FALSE,
  write_csv = FALSE,
  verbose = TRUE,
  max_rows_print = 20L,
  baseline_rds_path = NULL
)
}
\arguments{
\item{base_dir}{Character. Root directory containing the database folder.}

\item{db_name}{Character. SQLite database file name under
\code{file.path(base_dir, "database")}.}

\item{out_dir}{Character. Directory where diagnostic CSV files are written
when \code{write_csv = TRUE}.}

\item{station_ids}{Optional vector of Station IDs to include.}

\item{province}{Optional character vector of province names to include.}

\item{years}{Optional single year or two-value year range.}

\item{variables}{Character vector of Observation variable names to evaluate.}

\item{canada_ranges_rds_path}{Character. Path to Canada-wide baseline range
RDS file (1980-2020).}

\item{province_ranges_rds_path}{Character. Path to province baseline range
RDS file (1980-2020).}

\item{include_station_breakdown}{Logical. If \code{TRUE}, includes a larger
table with one row per station-variable pair.}

\item{include_outlier_rows}{Logical. If \code{TRUE}, includes a row-level
table of observations outside the \eqn{\mu \pm 3\sigma} range.}

\item{write_csv}{Logical. If \code{TRUE}, writes diagnostic CSV files.
Default is \code{FALSE}.}

\item{verbose}{Logical. If \code{TRUE}, prints diagnostic tables to the
R console. Defaults to \code{TRUE}.}

\item{max_rows_print}{Integer. Maximum rows to print for detailed tables
shown in detailed printed tables. Defaults to \code{20}.}

\item{baseline_rds_path}{Character. Optional override RDS path for the
baseline ranges used in the diagnostics.}
}
\value{
A named list with \code{range_scope_summary},
  \code{out_of_range_by_variable}, and (optionally)
  \code{out_of_range_by_station_variable} and
  \code{out_of_range_observations}. The return value is invisible.
}
\description{
Flags unexpected values by comparing Observation variables against a baseline
\eqn{\mu \pm 3\sigma} range from a reference profile (for example,
historical 1980-2020 data). This is intended as a general warning tool, not
a strict physical-validity check.
}
