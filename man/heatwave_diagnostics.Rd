% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Heat_Warning.R
\name{heatwave_diagnostics}
\alias{heatwave_diagnostics}
\title{Detect Heatwaves Using Provincial Baseline Temperature Ranges}
\usage{
heatwave_diagnostics(
  base_dir = getwd(),
  db_name = "climate_database.db",
  out_dir = file.path(base_dir, "drifloon_output", "diagnostics"),
  station_ids = NULL,
  province = NULL,
  years = NULL,
  min_consecutive_days = 2L,
  max_sd_threshold = 5,
  avg_sd_threshold = 3,
  canada_ranges_rds_path = file.path(base_dir, "data",
    "variable_ranges_Canada_1980-2020.rds"),
  province_ranges_rds_path = file.path(base_dir, "data",
    "variable_ranges_province_1980-2020.rds"),
  fallback_to_canada = TRUE,
  write_csv = FALSE,
  verbose = TRUE,
  max_rows_print = 20L
)
}
\arguments{
\item{base_dir}{Character. Root directory containing the database folder.}

\item{db_name}{Character. SQLite database file name under
\code{file.path(base_dir, "database")}.}

\item{out_dir}{Character. Directory where diagnostic CSV files are written
when \code{write_csv = TRUE}.}

\item{station_ids}{Optional vector of Station IDs to include.}

\item{province}{Optional character vector of province names to include.}

\item{years}{Optional single year or two-value year range.}

\item{min_consecutive_days}{Integer. Minimum consecutive heatwave days for an
event. Defaults to \code{2}.}

\item{max_sd_threshold}{Numeric. SD multiplier for the daily-maximum rule.
Defaults to \code{5}.}

\item{avg_sd_threshold}{Numeric. SD multiplier for the daily-average rule.
Defaults to \code{3}.}

\item{canada_ranges_rds_path}{Character. Path to Canada-wide baseline range
RDS file (1980-2020).}

\item{province_ranges_rds_path}{Character. Path to province baseline range
RDS file (1980-2020).}

\item{fallback_to_canada}{Logical. If \code{TRUE}, stations without a
matching province baseline use the Canada-wide baseline for Temp_C.}

\item{write_csv}{Logical. If \code{TRUE}, writes diagnostic CSV files.}

\item{verbose}{Logical. If \code{TRUE}, prints summary tables.}

\item{max_rows_print}{Integer. Maximum rows to print from detailed tables.}
}
\value{
A named list with \code{scope_summary}, \code{station_summary},
  \code{heatwave_events}, \code{heatwave_days}, and baseline metadata.
  The return value is invisible.
}
\description{
Detects heatwave events from daily temperatures in the Observation table.
A heatwave day is defined as a day where either daily maximum temperature is
at least 
\eqn{\mu + 5\sigma} or daily average temperature is at least
\eqn{\mu + 3\sigma}, where \eqn{\mu} and \eqn{\sigma} are taken from the
baseline range profiles. A heatwave event is two or more consecutive
heatwave days.
}
\details{
Provincial baseline ranges are used by default; when a station province is
missing from the province baseline file, the function can fall back to the
Canada-wide baseline.
}
